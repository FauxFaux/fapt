sudo: false
dist: trusty

language: rust
rust:
  - nightly
  - beta
  - stable

matrix:
  allow_failures:
    - rust: nightly

addons:
  apt:
    packages:
    - libgpg-error-dev
    - libgpgme11-dev
